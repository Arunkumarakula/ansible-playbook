#using playbook installing Nginx in node
- name: install nginx and run
  hosts: mongodb
  become: yes
  tasks:
  - name: Install NGINX
    ansible.builtin.yum:
    name: nginx
    state: installed
  - name: start NGINX
    ansible.builtin.service:
    name: nginx
    state: started
